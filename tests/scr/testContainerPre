#!/bin/bash

TEST_VOL1=$1

# Create a temp directory to bind mount into the privileged container's /var/lib/docker; this is required
# so that the sysvisor-runc instance inside the privileged container does not run on top of overlayfs
mkdir -p $TEST_VOL1
